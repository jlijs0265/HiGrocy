<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zerock.mapper.PMListMapper">

  <select id="getList" resultType="org.zerock.domain.PMListVO">
  	SELECT * FROM pm_list 
  </select>

	<insert id="insertPM" parameterType="org.zerock.domain.PMListVO">
		insert into pm_list (pm_list_code, machine_code, name, factory_name, location)
		VALUES(seq_pm_list_code.nextval, #{machine_code},  #{name}, #{factory_name}, #{location})
	</insert>
	
	<update id="updatePM" parameterType="org.zerock.domain.PMListVO">
		update pm_list set
		machine_code = #{machine_code}, name = #{name}, factory_name = #{factory_name}, location = #{location}
		where pm_list_code = #{pm_list_code} 
	</update>
	
	<delete id="deletePM" parameterType="int">
		delete pm_list
		where pm_list_code = #{pm_list_code}
	</delete>

</mapper>
