<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zerock.mapper.orderRecordMapper">

	<select id="getList" resultType="org.zerock.domain.orderRecordVO">
		SELECT * FROM order_record
	</select>
	
	<select id="getListOnce" resultType="org.zerock.domain.orderRecordVO">
  		SELECT * FROM order_record where order_code = #{order_code}
  	</select>

	<insert id="insertOR" parameterType="org.zerock.domain.orderRecordVO">
		INSERT INTO order_record (order_code, account_code, order_date,
		order_manager, delivery_date)
		VALUES(#{order_code}, #{account_code}, sysdate, #{order_manager}, sysdate)
	</insert>

	<update id="updateOR" parameterType="org.zerock.domain.orderRecordVO">
		update order_record set
		account_code = #{account_code}, order_date = #{order_date}, order_manager = #{order_manager}, delivery_date = #{delivery_date}
		where order_code = #{order_code} 
	</update>
	
	<delete id="deleteOR" parameterType="int">
		delete from order_record
		where order_code = #{order_code}
	</delete>


</mapper>